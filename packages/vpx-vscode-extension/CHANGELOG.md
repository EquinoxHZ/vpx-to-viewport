# Changelog

All notable changes to the VPX CSS Helper extension will be documented in this file.

## [0.1.0] - 2025-12-05

### Added
- Smart completions for `vpx`, `maxvpx`, `minvpx`, `cvpx` units
- Intelligent number+letter parsing (e.g., `100v` → `100vpx`)
- Snippet completions for `linear-vpx()` function (2-param and 4-param forms)
- Hover documentation with detailed explanations and examples
- Syntax highlighting (purple for units, yellow for functions)
- Support for CSS, SCSS, LESS, Vue, JSX, and TSX files
- Configuration options: `enableCompletions` and `enableHover`

